import styled from "styled-components";
import { motion } from "framer-motion";

export const MapWrapper = styled(motion.div)`
  width: 100%;
  height: auto;
`;

export const MapArea = styled(motion.div)``;
